{
  "name": "namui",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "npm run build -- --watch",
    "build": "esbuild index.ts --bundle --minify --sourcemap --outfile=build/bundle.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/dom-mediacapture-record": "^1.0.7",
    "esbuild": "^0.12.9"
  }
}
